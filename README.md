# collaborative-filtering
##Overview
This project implements a user-based collaborative filtering model for movie recommendations.
A user–item rating matrix is constructed, normalized, and used to compute similarities and generate recommendations for new users.
